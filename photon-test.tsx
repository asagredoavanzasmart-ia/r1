import React from 'react';
import ReactDOM from 'react-dom/client';
import PhotonTestPage from './components/PhotonTestPage';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <PhotonTestPage />
    </React.StrictMode>,
);
